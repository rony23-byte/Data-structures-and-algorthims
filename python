class Solution(object):
    def containsDuplicate(self, nums):
        hashset=set()
        for n in num:
            if n in hashset:
                return True
            else:
                hashset.add(n)
        return False    
            
         
      
